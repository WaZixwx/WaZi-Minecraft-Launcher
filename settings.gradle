// 添加插件仓库
pluginManagement {
    repositories {
        gradlePluginPortal()
        maven { url 'https://mirrors.cloud.tencent.com/gradle/' }
        maven { url 'https://services.gradle.org/distributions/' }
        google()
        mavenCentral()
    }
    resolutionStrategy {
        eachPlugin {
            if (requested.id.namespace == 'org.openjfx') {
                useModule("org.openjfx:javafx-plugin:${requested.version}")
            }
        }
    }
}

rootProject.name = 'WaZi-Minecraft-Launcher' 